# Word 边栏 Agent 实现方案

## 目标
做一个集成在 Microsoft Word 里的边栏 Agent，体验尽量对齐 Word Copilot：
- 用户在 Word 内直接打开侧边栏进行对话
- 支持用户填写 OpenAI 兼容 API 的 `base_url` 和 `api_key`，由本机直接调用用户自己的模型订阅
- 支持上传文件作为知识库，供当前会话检索和问答
- 能对当前文档进行可控、可审阅的编辑，而不是只生成文本
- 首版优先保证高可行性、低风险、易交付

## 总体结论
最可行的路线是：
- 前端采用 Word Add-in 任务窗格，作为边栏入口
- Agent 运行在用户本机的常驻服务或本地进程中，不依赖云端中转
- 模型层直接调用用户配置的 OpenAI 兼容接口，数据流从本机发出并返回本机
- 知识库采用“上传 -> 切块 -> 向量化 -> 检索”的标准 RAG 流程，索引默认保存在本地
- 文档修改采用“计划 + 预览 + 应用”的两阶段方式，保证稳定和可撤销

这个方案的优势是：
- 能真正嵌入 Word，用户感知接近 Copilot
- 通过 Word API 修改文档，比模拟鼠标键盘稳定得多
- 能兼容 OpenAI 及类 OpenAI 服务，便于用户按自己的订阅切换模型
- 文档、知识库和密钥默认留在用户机器上，更符合本地插件预期
- 先做 Word，可快速形成可用 MVP

## 推荐技术路线
### 首选：Office Add-in + 本地 Agent
这是最适合首版的方案，也是最贴近你构想的落地方式。

前端：
- Office.js Task Pane
- React + TypeScript
- 任务窗格内实现聊天、知识库上传、设置页、文档操作确认页

本地 Agent：
- Node.js 或 Python 常驻服务，安装在用户机器本地
- 负责模型调用、RAG 检索、工具编排、编辑计划生成
- 对外暴露本地回环地址 API 或本地 IPC，供 Add-in 调用

文档操作：
- Word JavaScript API 读取正文、选区、段落、表格
- 通过受控命令执行插入、替换、重写、格式调整
- 保留撤销路径和修改预览

### 不建议首版直接走的路线
- UI 自动化模拟点击：不稳定，且容易受 Word 版本和焦点影响
- 深度改造 Word 本体：成本高、发布难、风险大
- 仅做独立网页助手：和 Word 的沉浸感差，无法达到 Copilot 体验

## 用户体验目标
要尽量贴近 Word Copilot，可以拆成 5 个核心体验点：
1. 侧边栏常驻，用户无需切换应用
2. 可以直接引用当前文档内容作为上下文
3. 支持对选中文本进行“改写、总结、扩写、润色”
4. 支持知识库问答，回答时能引用上传文件
5. 修改前先预览，用户确认后再应用到文档

## 功能范围
### 第一阶段 MVP
- 登录/连接配置页
- OpenAI 兼容 `base_url` 和 `api_key` 配置
- 聊天式侧边栏
- 读取当前文档和选区
- 选中文本一键改写、总结、翻译、缩写
- 上传知识库文件并可检索问答
- 将回答插入当前光标位置
- 修改前差异预览
- 一键应用 / 撤销

### 第二阶段增强
- 按段落、标题、表格进行结构化编辑
- 根据文档类型生成写作建议
- 多轮编辑任务，例如“把这一节改成更正式的商务风格”
- 支持文档内引用知识库内容并标注来源
- 支持生成摘要、目录、行动项、会议纪要

### 第三阶段进阶
- 支持图文混排、表格增强、批量替换
- 支持企业知识库权限隔离
- 支持团队共享提示词模板
- 支持工作流自动化，比如“收到文档后先总结再补充”

## 架构设计
### 1. Word 前端边栏
职责：
- 呈现对话界面
- 上传知识库文件
- 展示配置项
- 展示文档上下文和编辑预览
- 触发 Word 选区读取、应用修改、撤销修改

建议组件：
- Chat panel
- File uploader
- Settings panel
- Context viewer
- Diff preview panel
- Action buttons

### 2. Agent 后端
职责：
- 接收前端消息和文档上下文
- 调用 OpenAI 兼容模型
- 生成工具调用计划
- 做 RAG 检索
- 输出结构化编辑指令

建议模块：
- Conversation Orchestrator
- LLM Adapter
- Retrieval Service
- Document Planner
- Edit Executor
- Session Store

部署形态建议：
- 作为本地后台进程常驻运行
- 仅监听 `127.0.0.1`，不对局域网开放
- 由 Word Add-in 通过本机地址或本地 socket 通信

### 3. 知识库服务
职责：
- 接收上传文件
- 解析文本和结构
- 分块
- 向量化
- 支持语义检索和来源返回

存储建议：
- 文件原文、切块文本、embedding 和会话缓存都默认存本地
- 允许用户选择是否清理会话缓存和知识库索引

建议支持：
- PDF
- DOCX
- TXT
- Markdown
- PPTX 作为后续扩展

### 4. 文档执行层
职责：
- 通过 Word API 读取当前内容
- 根据编辑计划执行增删改
- 返回执行结果和错误信息

关键原则：
- 不直接让模型输出“最终操作结果”
- 先生成结构化计划，再由执行层落地
- 保留审阅和回滚能力

## 核心数据流
### 对话流程
1. 用户在侧边栏输入指令
2. 前端收集当前文档上下文、选区、会话状态
3. 本地 Agent 结合知识库进行检索
4. 模型生成回答或编辑计划
5. 前端展示预览
6. 用户确认后执行到 Word
7. 执行结果回写到会话

### 上传知识库流程
1. 用户上传文件
2. 后端解析文件内容
3. 切块并生成向量索引
4. 与当前会话绑定
5. 后续提问时进行检索增强
6. 回答附带引用来源

## OpenAI 兼容配置设计
用户配置界面建议只保留最少必填项：
- `base_url`
- `api_key`
- `model`
- 可选的 `temperature`
- 可选的 `max_tokens`

建议兼容以下场景：
- 官方 OpenAI
- Azure OpenAI 的兼容代理层
- 其他提供 OpenAI API 兼容接口的供应商

实现建议：
- 本地 Agent 统一封装一个 OpenAI-compatible client
- Word 边栏只负责配置和触发，不直连模型服务
- 密钥本地加密存储，优先使用系统安全存储或受保护的本地凭据库
- 允许用户随时切换 base_url 和 model，保持“自己的订阅自己控制”

## 文档编辑策略
这是能否接近 Copilot 体验的关键。

### 1. 先规划，后执行
模型输出三层信息：
- 目标：要达成什么
- 操作：需要修改哪些段落/文本/表格
- 预览：修改后的结果片段

### 2. 支持局部编辑
优先支持这些操作：
- 替换选中文本
- 改写段落
- 插入新段落
- 生成总结段
- 添加标题/小节

### 3. 保留人类确认
所有会改动正文的动作都建议默认走确认：
- 先展示 diff
- 再应用
- 提供撤销

### 4. 约束模型输出
编辑计划应为结构化 JSON，而不是自由文本。
例如：
- target range
- operation type
- replacement text
- formatting hints
- source references

## 知识库设计
### 基础方案
- 文件上传后按文档类型解析内容
- 按语义块切分
- 生成 embedding
- 使用向量数据库检索

### 推荐实现
首版可以用以下任一方式：
- 本地 SQLite + 向量扩展
- Qdrant
- pgvector
- Chroma

如果你想优先高可行性和快速上线，推荐：
- 开发期：Chroma 或 SQLite 系方案
- 产品期：pgvector 或 Qdrant

### 检索策略
- 先做语义召回
- 再做重排
- 返回片段、来源、页码或段落位置
- 在回答中强制引用来源，减少幻觉

## 安全与合规
这类产品必须提前考虑：
- API key 是否本地保存
- 文档和知识库是否完全不出本机
- 是否提供审计日志
- 是否记录知识库访问权限

建议默认策略：
- 文档、知识库、会话状态和 key 默认不出本机
- 上传知识库前给出清晰提示，明确说明本地索引范围
- 提供“完全本地模式”，云端只作为用户自己填写的模型终点，不作为中转或托管
- 本地缓存可手动清理，并提供一键退出后清空会话数据

## MVP 落地顺序
### 第 1 步
做 Word 任务窗格 + 配置页 + 聊天页 + 本地 Agent 连接层。

### 第 2 步
接入 OpenAI 兼容模型，实现基本问答和选区改写，支持用户自填 base_url 和 key。

### 第 3 步
实现知识库上传、解析、检索和引用，索引与缓存默认只保存在本机。

### 第 4 步
实现文档局部编辑、预览和应用。

### 第 5 步
增强 Word 内结构化编辑能力和多轮任务能力。

## 本地化边界
为了和“本地插件”构想一致，建议把边界定义得更明确：
- Word Add-in 只是 UI 和文档入口，不承担重计算
- 本地 Agent 负责全部推理、检索和编辑规划
- 模型请求直达用户自己的订阅，不经过你的云端
- 知识库索引、会话记忆和编辑历史默认只保存在本机
- 如果以后要做同步能力，再作为显式可选功能单独打开

## 推荐的首版落点
如果你要尽快做出一个高可行性版本，我建议首版只做：
- Word 侧边栏聊天
- OpenAI 兼容配置
- 本地知识库上传和检索
- 选区改写与插入
- 预览和撤销

先不要做：
- 多端同步
- 云端账号体系
- 团队共享知识库
- 跨设备会话接续
- 复杂的企业权限系统

## 风险点
- Word API 对某些复杂格式支持有限
- 不同 Office 版本兼容性有差异
- 直接修改整篇文档容易破坏格式
- 知识库问答容易过度依赖模型生成
- 上传大文件后解析性能可能成为瓶颈

## 规避策略
- 优先局部编辑，不做整文重写
- 所有修改都保留预览和撤销
- 文档解析和 embedding 异步化
- 先做 Word，不把三件套同时铺开
- 模型输出严格结构化，减少自由发挥

## 方案判断
如果目标是尽快做出一个“像 Word Copilot 的边栏 Agent”，最推荐的方案是：
- Word Add-in 作为前端
- 独立 Agent 后端
- OpenAI 兼容模型接入
- RAG 知识库
- 结构化编辑计划 + 预览确认 + Word API 执行

这条路线的可行性最高，且最适合先做 MVP。

## 下一步建议
如果你愿意，我可以继续把这个方案细化成下面任意一种：
- 一份更细的技术架构图
- 一个可直接开工的目录结构
- Word Add-in 的页面原型说明
- 后端 API 设计文档
- MVP 开发排期
